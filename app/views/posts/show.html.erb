<h1>Post</h1>
<%= @post.body %>
<ul>
<% @comments.each do |comment| %>
    <li><%= comment.body %> // <%= link_to 'Show Comment', comment_path(comment) %> | <%= link_to 'Edit Comment', edit_comment_path(comment) %> | <%= link_to 'Delete Comment', comment_path(comment), method: :delete %> </li>
<% end %>
</ul>
<%= link_to 'Add New Comment', new_comment_path(post_id: @post.id) %>